# Inner Product via zip (QuantEcon 5.7.1, Part 1)

Given two numeric lists or tuples `x_vals` and `y_vals` of equal length, compute their inner product using `zip()`.

Define a function `inner_product(x_vals, y_vals)` that returns the sum of element-wise products. Use `zip()` in your implementation.
